import Events from '../src/events';

test('imports', function(){
    ok(typeof Events === 'function'); 
});

module("sdom.events", {
    setup: function() {
    },
    teardown: function() {
    }
});
